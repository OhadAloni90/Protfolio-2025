import React from 'react'
interface BoundryWallProps {
    position: [number, number, number];
    scale: [number, number, number];
    transparent: boolean;
}
const BoundryWall: React.FC<BoundryWallProps> = ({ position, scale, transparent }) => {
    
  return (
    <div>
      
    </div>
  )
}

export default BoundryWall
